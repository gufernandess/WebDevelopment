const MotherBoard = ({name, price}) => {
    return(
        <>
            <h1>Name: {name}, Price: {price}</h1>
        </>
    );
};

const Memory = ({name, price}) => {
    return(
        <>
            <h1>Name: {name}, Price: {price}</h1>
        </>
    );
};

const VideoBoard = ({name, price}) => {
    return(
        <>
            <h1>Name: {name}, Price: {price}</h1>
        </>
    );
};

export {
    MotherBoard,
    Memory,
    VideoBoard
};